<%= form_for task, remote: true do |f| %>
  <%= f.check_box :complete %>
  <%= f.submit "Update" %>
  <%= f.text_field :name %>
  <%= link_to "Delete", task, method: :delete, remote: true%>
<% end %>
